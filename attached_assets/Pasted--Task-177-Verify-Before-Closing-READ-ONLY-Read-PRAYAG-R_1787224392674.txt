# Task #177 — Verify Before Closing (READ-ONLY)

Read `PRAYAG_RULES.md`. **Change nothing.** No Build mode, no restart, no publish, no Sheets writes (**R-21**), no cache clearing (**R-34**). If something is wrong, **report it — do not fix it** (**R-30**).

Five checks. #177 is not finished until all five pass.

---

## 1 — Inert by default

The whole design rests on this: with `week_days` unset, nothing changes.

Run a Plumbing plan with no `week_days` configured and confirm the output is **byte-identical** to before the change — same `total_days` (25), same `week_days` `[6,6,6,7]`, same weekly capacity split, same blocks, same unfinished list, same `total_excess_kg`.

Then confirm the corrective re-planner also falls back to its original Mon–Sat calendar when `week_days` is unset, so its projections are unchanged too.

## 2 — Segment isolation

`mp_params` is keyed by `(segment, effective_month)`.

- Confirm every write is scoped to `segment = "PLUMBING"`. Print the write path.
- Confirm **PTMT, Garden, HDPE, Tank and CP** plan runs are unchanged — same schedules, same capacities, same unfinished items.
- Confirm no other segment can read a Plumbing `week_days` value.

## 3 — The unfinished-demand filter

The message must use `remaining_kg` summed across unfinished items **where `downtime_reason` is empty**.

Items unfinished because their only capable machines were down are a breakdown problem — extending the month would not help them, and suggesting it would be wrong.

Report:
- the exact expression used
- a case with a downtime-blocked item only → **no** working-days message
- a case with capacity-limited unfinished demand → message fires, with the text as rendered
- confirmation the message sits **alongside** the existing unfinished warning rather than replacing it

## 4 — Rounding in the proportional spread

Proportional spreading must be deterministic and must not drift.

- Confirm `elapsed + remaining == total` for **every** date in the month, for a 25-day configuration in a 31-day month and for a full-calendar-month configuration.
- Confirm the same month and the same `as_of` date always produce the same counts.
- State the rounding rule used.

## 5 — Nothing outside scope moved

- **`total_excess_kg`** unchanged in meaning, unchanged on the KPI tile, unchanged in the PDF. It measures minimum-shift-block overrun and is not a capacity signal.
- **No ideal-hours figure changed anywhere.** `baselines.json`, `APP_DEFAULT_IDEAL_HOURS` and the sheet-sourced ideal hours are all untouched.
- **No management report figure moved:** Pipe 1,283,300.15 / 6,507 h · Moulding 366,015.39 / 35,972 h · PTMT 524,467.607 net / 557,264.061 gross · Garden 262,818.23 · HDPE 23,817.24 · Tank litres and both rejection bases.
- `sheets.py`, `parsers.py`, `metrics.py`, `pipe_reconcile.py`, `tank_reconcile.py` untouched.

## 6 — Pushed

Confirm the work is committed **and pushed to `origin/main`**, with the working tree clean and local and remote in sync. Give the commit hash.

---

## Report back

The five checks with evidence, plus the commit hash. If any check fails, **report it rather than closing the task.**