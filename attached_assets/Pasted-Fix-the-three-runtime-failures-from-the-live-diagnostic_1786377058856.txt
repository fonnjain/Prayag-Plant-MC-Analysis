Fix the three runtime failures from the live diagnostic (PTMT fallback, Tank 0 Ltr, Garden labour) plus one related bug (Garden machine labels). Build mode. Add pytest per fix. Restart after edits. Never touch "/". Pre-FY26-27 (history/YoY) must stay untouched. All targets below are RECOMPUTED-FROM-MONTHLY values to validate against — parse by header, assert found, fail loud, never fabricate.

=== FIX 1 (highest value) — TANK: 0 Ltr → real litres ===
Two compounding bugs:
(a) sources.py sets tab='Sheet1' for all three tank_annual_2627 entries. Sheet1 is a 12-row stub. Real data is the 'DATA' tab. Change tab to 'DATA' for KH/VN/WB.
(b) parse_tank_annual_2627 expects month header + column labels in one row; the DATA tab is a WIDE layout — month labels span row 0 (one label every 7 columns: MAR-27, FEB-27 … APR-26), and column headers (ITEM CODE, COLOR, PRODUCTION IN PCS., PRODUCTION IN KG., REJECTION IN PCS., REJECTION IN KG., REJECTION MOUTH LID) sit in row ~2, offset far right. Rewrite the parser to: locate each month's 7-column block by the row-0 label, read Production (Ltr) and Rejection (Ltr) by the row-2 header within that block, map month by label (latest-first), sum item rows. Assert the month blocks and Ltr columns are found; if not, raise a named error (do NOT return []).
Validate (recomputed from DATA, summed KH+VN+WB):
  Per plant Apr–Jul (Ltr): KH 633,500 (JUN only) · VN 1,852,250 · WB 4,325,100
  Combined 6,810,850 Ltr / rejection 86,500 / by month APR 636,250 · MAY 1,582,500 · JUN 2,596,600 · JUL 1,995,500
  Unit stays Ltr; no labour/hours fabricated. KH-single-month + WB-inconsistency integrity flags still fire.

=== FIX 2 — PTMT: summary_tab_fallback → data_entry_tabs ===
Tab detection fails against the real workbook, so mc_tab=None → fallback → Run Moulds shows 0.
(a) mc_tab match: 'Month Wise M/C' contains a slash — "MC" is not a substring of "M/C". Match on "MONTH" AND "WISE" (slash-tolerant) instead of "MC".
(b) mould_tab match: add a "PTMT" prefix guard so "PTMT Mould Apr'26-Mar'27" is selected, NOT "Moulding M/C 26-27" (which currently matches first).
After the fix, print/confirm source_kind == "data_entry_tabs" (NOT summary_tab_fallback) and that parse_ptmt_monthly_mc_tab returns non-empty rows.
Validate (recomputed from data-entry tabs): Run Moulds APR 216 · MAY 280 · JUN 311 · JUL 298 · TOTAL 1,105. Hours/Nett Output/rejection/wages must stay at their current correct values (16,092 / 99,262 / 537,109 / 32,952 / 6.14% / ₹2,009,948); Nett Output basis = 537,109 (not 541,258/454,867). SUMMARY tab remains validation-only.

=== FIX 3 — GARDEN: labour join broken ===
_seg_labour_cache for segment "Garden Pipe" returns ym='' with labour=32/wages=₹90 — a header/total row is being ingested as data. Fix the seg_labour parse so the "Garden Pipe" segment yields real monthly rows (skip the blank/total/header row; require a valid ym). Expected: 56 headcount / ₹426,164 / ₹2.97 per kg (wages ÷ recomputed output kg over matching months).
After fix: MAY integrity flag (wages>0, output=0) must be able to fire; JUL flag (output>0, wages blank) still fires.

=== FIX 4 — GARDEN: empty machine_label (per-machine rejection unverifiable) ===
parse_mc_detail returns machine_label='' for every Record from this workbook, so per-machine rejection can't be produced. Fix the machine-label carry-forward/extraction so M/C-1..4 are populated.
Validate per-machine APR rejection: M/C-1 2.23% · M/C-2 2.81% · M/C-3 3.00% · M/C-4 10.48%.
Also investigate the JUL segment rejection: recompute shows 5.54% vs target 5.76% (0.22pp gap) — confirm whether a July rejection cell/row is being missed; report the cause (fix only if it's a parse miss, not a real source value).

=== TESTS ===
pytest per fix:
 - Tank: DATA tab parsed, wide layout handled, combined == 6,810,850, per-plant match, named error on missing headers.
 - PTMT: source_kind == data_entry_tabs, Run Moulds total == 1,105, production/wages unchanged, SUMMARY not primary.
 - Garden labour: "Garden Pipe" yields ym-valid rows, total ₹426,164 / ₹2.97 per kg, MAY flag fires.
 - Garden machine_label: M/C-1..4 populated, APR per-machine rejections match; JUL rejection cause reported.
Confirm "/" unaffected and pre-FY26-27 history/YoY unchanged.