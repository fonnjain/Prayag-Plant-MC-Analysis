Four independent fixes from the live diagnostic + investigation. Build mode, pytest per fix, restart after edits. Never touch "/". Pre-FY26-27 (history/YoY) stays untouched. Governing rule: FY26-27 figures come from monthly data-entry tabs only; annual SUMMARY = validation + styling only. Parse by header text, assert found, fail loud (raise a named error, never return [] or a silent fallback), never fabricate a number to hit a target.

=========================================================
FIX 1 — TANK: 0 Ltr → real litres  (RECONCILE BEFORE WIRING)
=========================================================
Two known bugs: sources.py tab='Sheet1' (a 12-row stub) for all three tank_annual_2627 entries; and parse_tank_annual_2627 can't read the wide layout.

DO NOT assume 'DATA' is the master tab. The DATA row-1 totals did NOT reconcile with the VN 1,852,250 target in prior investigation. So FIRST reconcile, THEN wire:

STEP A — determine the true monthly source. For VN (1Wa2jFV66NS-ntlSKqo8jzFFwgZfcdvgJYEAuFU0qdAI), compute the Apr–Jul monthly Ltr totals THREE ways and print all three:
  (i)  DATA tab: sum ALL item rows within each month-block (wide layout — month labels span row 0, one every 7 cols: MAR-27…APR-26; column headers Production in Ltr / Rejection in Ltr in row ~2, offset right per block). Row 1 is NOT a total — it is the first item; you must sum every item row in the block.
  (ii) per-size tabs (500 LTR / 750 LTR / 1000 LTR / …): sum each size's monthly Ltr.
  (iii) SUMMARY (LTR) tab TOTAL row per month (this is the validation reference, not the source).
  Target to match: VN APR 221,250 · MAY 534,000 · JUN 533,500 · JUL 563,500 = 1,852,250.
  Report which of (i)/(ii) reconciles to (iii). Whichever reconciles is the FY26-27 source tab.

STEP B — wire sources.py to the reconciling tab (DATA if it reconciles; else the per-size tabs) for KH/VN/WB, and rewrite parse_tank_annual_2627 to read that layout by header, mapping month by label (latest-first), summing item/size rows per month-block. Assert month-blocks + Ltr columns found; raise named error if not.

Validate (summed KH+VN+WB): per plant KH 633,500 (JUN only) · VN 1,852,250 · WB 4,325,100; combined 6,810,850 Ltr / rej 86,500; by month APR 636,250 · MAY 1,582,500 · JUN 2,596,600 · JUL 1,995,500. Unit stays Ltr; no labour/hours fabricated. KH-single-month + WB-inconsistency (item-strip Jul 2,198 vs TOTAL 1,834 pcs / 1,432,000 Ltr; Jun 1,424,600 vs 1,429,600) flags fire; use TOTAL-row figures. File IDs already correct — VN reads VN, do not change them.

=========================================================
FIX 2 — PTMT: summary_tab_fallback → data_entry_tabs
=========================================================
Live source_kind is summary_tab_fallback (reading the annual). Two tab-name mismatches:
 (a) mc_tab: code tests "MC" in tab, but tab is 'Month Wise M/C' — the slash means "MC" is not a substring of "M/C". Match on "MONTH" AND "WISE" (slash-tolerant).
 (b) mould_tab: add a "PTMT" prefix guard so "PTMT Mould Apr'26-Mar'27" is chosen, NOT "Moulding M/C 26-27" (currently matched first).
After fix, PRINT and confirm source_kind == "data_entry_tabs" (not fallback) and parse_ptmt_monthly_mc_tab returns non-empty rows.
Validate: Run Moulds APR 216 · MAY 280 · JUN 311 · JUL 298 · TOTAL 1,105. Hours/Nett Output/rejection/wages must stay correct (75,083 / 537,109 / 32,952 / 6.14% / ₹2,009,948); Nett Output basis = 537,109 (not 541,258/454,867). SUMMARY stays validation-only.

=========================================================
FIX 3 — GARDEN labour: FIRST undo the false fix, THEN fix properly
=========================================================
Task #119 (commit 6e057ef) marked itself IMPLEMENTED but did NOT fix the bug: parse_segment_labour still maps hcol("TOTAL") to col 5 "Total Labour" = HEADCOUNT (31), and the garden filter still matches the combined UNIT-3 segment label ('1. GARDEN PIPE,\n&\n2. HDPE PIPE,\n&\n3. TANK'). So displayed "wages" are headcount, and solar_c hits "Solar Generation Unit (kWh)". 
STEP A — review 6e057ef; revert any change that is cosmetic/non-functional for garden wages so the codebase isn't left in a false-done state.
STEP B — real fix: Garden wages live in the DEDICATED "Garden Pipe" tab (APR: Paid Wages 76,772 + Contractor 50,547). Add parse_segment_named_tab, read the "Garden Pipe" tab in the seg_labour branch, and change the app.py filter to EXACT match "Garden Pipe" (not the UNIT-3 combined segment).
VALIDATE BEFORE WIRING: confirm the named-tab Apr–Jul sum reconciles to 56 headcount / ₹426,164 / ₹2.97 per kg (wages ÷ recomputed output). If it doesn't reconcile, stop and report — do not wire a number that misses the target.
After fix: MAY integrity flag (wages>0, output=0) can fire; JUL flag (output>0, wages blank) still fires.

=========================================================
FIX 4 — GARDEN machine labels (per-machine rejection)
=========================================================
parse_mc_detail returns machine_label='' for every Record, so per-machine rejection can't render. Fix the machine-label carry-forward/extraction so M/C-1..4 populate.
Validate APR per-machine rejection: M/C-1 2.23% · M/C-2 2.81% · M/C-3 3.00% · M/C-4 10.48%.
Also: recomputed JUL segment rejection is 5.54% vs annual 5.76% (0.22pp). INVESTIGATE the cause and report it; fix ONLY if it is a parse miss. If 5.54% is what the July monthly rows genuinely compute to, the monthly value is authoritative — do NOT bend it to match the annual.

=========================================================
TESTS (per fix) + report
=========================================================
Tank: reconciliation table (DATA vs per-size vs SUMMARY) printed; source tab = the reconciling one; combined == 6,810,850; per-plant match; named error on missing headers.
PTMT: source_kind == data_entry_tabs; Run Moulds total == 1,105; production/wages unchanged.
Garden labour: 6e057ef false-fix addressed; "Garden Pipe" named tab yields total ₹426,164 / ₹2.97 per kg; MAY flag fires.
Garden labels: M/C-1..4 populated; APR per-machine rejections match; JUL 5.54-vs-5.76 cause reported.
Confirm "/" unaffected and pre-FY26-27 untouched. Do not mark any task implemented unless its validate step actually passes against the target.